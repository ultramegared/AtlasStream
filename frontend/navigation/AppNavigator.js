/**
 * AtlasStream
 * App Navigator
 *
 * Controls the main application flow.
 */

const AppNavigator = () => {
  return {
    name: "AppNavigator",
    status: "Pending",
    description: "Main navigation controller."
  };
};

export default AppNavigator;